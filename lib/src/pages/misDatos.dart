import 'package:flutter/material.dart';

class MisDatos  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mis Datos', textScaleFactor: 2.0),
      
    );
  }
}