import 'package:flutter/material.dart';

class ContactoPostLogin  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Contacto', textScaleFactor: 2.0),
      
    );
  }
}